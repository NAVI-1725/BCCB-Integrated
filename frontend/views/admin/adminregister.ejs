<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Register</title>
    <!-- Bootstrap CSS for consistent styling -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">

    <style>
        @keyframes move-background {
            from {
                background-position: 0 0;
            }
            to {
                background-position: 100% 100%;
            }
        }

        body::before {
            content: '';
            position: absolute;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, #d4e8f9, #a8cde8); /* Elegant blue gradient */
            z-index: -1;
            animation: move-background 20s linear infinite;
        }

        .register-page {
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            background: linear-gradient(135deg, #d4e8f9, #a8cde8);
            position: relative;
            overflow: hidden;
        }


        @keyframes slide-in {
            from {
                transform: translateY(-50px);
                opacity: 0;
            }
            to {
                transform: translateY(0);
                opacity: 1;
            }
        }

        .register-box {
            width: 100%;
            max-width: 510px;
            background-color: rgba(255, 255, 255, 0.5);
            border-radius: 30px;
            padding: 35px;
            box-shadow: 0 20px 50px rgba(0, 0, 0, 0.3);
            backdrop-filter: blur(10px);
            animation: floating 3s ease-in-out infinite;
            position: relative;
            overflow: hidden;
        }

        .register-box:hover {
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.4);
        }

        @keyframes floating {
            0%, 100% {
                transform: translateY(0px);
            }
            50% {
                transform: translateY(-10px);
            }
        }

        .bubble {
            position: absolute;
            bottom: -20px;
            width: 30px;
            height: 30px;
            border-radius: 50%;
            background-color: rgba(255, 255, 255, 0.6);
            animation: float-bubbles 5s ease-in-out infinite;
        }

        @keyframes float-bubbles {
            0% {
                opacity: 0.7;
                transform: translateY(0) scale(1);
            }
            50% {
                opacity: 0.4;
                transform: translateY(-50px) scale(1.5);
            }
            100% {
                opacity: 0.7;
                transform: translateY(-100px) scale(1);
            }
        }

        .bubble:nth-child(1) { left: 10%; animation-duration: 6s; }
        .bubble:nth-child(2) { left: 20%; animation-duration: 7s; }
        .bubble:nth-child(3) { left: 30%; animation-duration: 5s; }
        .bubble:nth-child(4) { left: 40%; animation-duration: 8s; }
        .bubble:nth-child(5) { left: 50%; animation-duration: 6s; }
        .bubble:nth-child(6) { left: 60%; animation-duration: 7s; }
        .bubble:nth-child(7) { left: 70%; animation-duration: 6s; }

        .text-colour-new {
            color: #4a84b1 !important; /* Red text */
        }

        .form-control {
            width: 80%;
            margin: 0 auto;
            transition: all 0.3s ease;
        }

        .form-control:focus {
            border-color: #a8cde8; /* Red border */
            box-shadow: 0 0 8px rgba(122, 178, 218, 0.5); /* Red glow */
        }

        .btn-register {
            background-color: #7096b5; /* Red button */
    border-color: #1f6ea7;
    transition: background-color 0.3s ease, transform 0.3s ease;
    width: 200px;
    display: block; /* Makes the button behave like a block element */
    margin: 20px auto; /* Centers the button horizontally */
        }

        .btn-register:hover {
            background: linear-gradient(135deg, #c7e2f9, #a0cae8);
            transform: scale(1.05);
            color: #000;
        }

    </style>
</head>
<body class="bg-gray-100">
    <header class="bg-blue-500 text-white p-4 text-center">
        <%- include('../partials/admin_loginheader.ejs') %>
    </header>

    <div class="register-page">
        <div class="register-box">
            <h2 class="text-center text-2xl font-bold mb-4">Admin Registration</h2>

            <form action="/admin/register" method="POST">
                <div class="form-group">
                    <label for="name" class="form-label">Full Name</label>
                    <input type="text" class="form-control mt-1" name="name" id="name" required>
                </div>
                <div class="form-group">
                    <label for="email" class="form-label">Email</label>
                    <input type="email" class="form-control mt-1" name="email" id="email" required>
                </div>
                <div class="form-group">
                    <label for="mobile" class="form-label">Mobile Number</label>
                    <input type="text" class="form-control mt-1" name="mobile" id="mobile" required>
                </div>
                <div class="form-group">
                    <label for="password" class="form-label">Password</label>
                    <input type="password" class="form-control mt-1" name="password" id="password" required>
                </div>
                <div class="form-group">
                    <label for="passcode" class="form-label">Admin Passcode</label>
                    <input type="password" class="form-control mt-1" name="passcode" id="passcode" required>
                    <small class="form-text text-muted mt-1">This passcode is required and is only known to authorized members.</small>
                </div>
                <button type="submit" class="btn btn-primary btn-block mt-3 btn-register">Register</button>
                <p class="text-center mt-3"><a href="/admin/login" class="text-colour-new hover:underline">Already have an account? Login here</a></p>
            </form>                
        </div>
        <div class="bubble"></div>
        <div class="bubble"></div>
        <div class="bubble"></div>
        <div class="bubble"></div>
        <div class="bubble"></div>
        <div class="bubble"></div>
        <div class="bubble"></div>
    </div>

    <footer class="bg-gray-800 text-white p-4 text-center">
        <%- include('../partials/footer.ejs') %>
    </footer>

    <!-- Bootstrap JS, Popper.js, and jQuery -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <script src="/js/main.js"></script>
</body>
</html>
