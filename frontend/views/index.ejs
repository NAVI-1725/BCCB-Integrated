<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome to Your Website</title>
    <!-- Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="../css/styles.css"> <!-- Ensure this file exists -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css"> <!-- Font Awesome Icons -->
    <style>
        /* Background and Bubble Animation Styles */
        body {
            margin: 0;
            padding: 0;
            height: 100vh;
            overflow: hidden;
            background: linear-gradient(135deg, #a8e0e8, #a8cde8); /* Light blue gradient */
            font-family: 'Roboto', sans-serif;
            position: relative;
        }
        .bubbles {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            overflow: hidden;
            z-index: 1;
        }

        .bubble {
            position: absolute;
            bottom: -150px;
            width: 40px;
            height: 40px;
            background: linear-gradient(135deg, #caefef, #c7e0ee);
            border-radius: 50%;
            animation: floatUp 10s infinite ease-in-out;
        }

        @keyframes floatUp {
            0% {
                transform: translateY(0) scale(1);
            }
            50% {
                transform: translateY(-600px) scale(1.2);
            }
            100% {
                transform: translateY(-1000px) scale(0.8);
            }
        }

        .bubble:nth-child(2) {
    width: 60px;
    height: 60px;
    animation-duration: 12s;
}

.bubble:nth-child(3) {
    left: 25%;
    width: 80px;
    height: 80px;
    animation-duration: 15s;
}

.bubble:nth-child(4) {
    left: 50%;
    width: 50px;
    height: 50px;
    animation-duration: 11s;
}

.bubble:nth-child(5) {
    left: 75%;
    width: 100px;
    height: 100px;
    animation-duration: 20s;
}


        .container {
            position: relative;
            z-index: 2;
        }

        h1 {
            font-size: 3.5rem; /* Increased size */
            font-weight: bold;
            color: #084b83; /* Darker blue for contrast */
        }

        p {
            font-size: 1.5rem; /* Larger size for readability */
            color: #053f5e;
            margin-bottom: 30px;
        }

        .btn {
            font-size: 1.2rem;
            padding: 12px 24px;
        }

        /* Primary Button */
.btn-primary {
    color: black; /* Retain black text */
    border: none !important; /* Remove border */
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1) !important; /* Subtle shadow for depth */
    transition: transform 0.3s ease, box-shadow 0.3s ease, background 0.3s ease; /* Smooth transition for transform, shadow, and background */
    background: linear-gradient(135deg, #d4f1f9, #a8e0e8); /* Keep the same gradient */
    padding: 12px 25px; /* Increased padding for better button size */
    border-radius: 8px; /* Rounded corners */
    cursor: pointer; /* Pointer cursor for interactivity */
    margin: 10px; /* Space between buttons */
}

.btn-primary:hover {
    transform: translateY(-4px); /* Slight lift effect on hover */
    color: #cd2631 ;
    box-shadow: 0 6px 12px rgba(0, 0, 0, 0.15); /* Stronger shadow on hover */
    background: linear-gradient(135deg, #a8e0e8, #d4f1f9); /* Slightly darker gradient on hover */
}

.btn-primary:focus {
    outline: none; /* Remove default outline */
    box-shadow: 0 0 0 3px rgba(76, 198, 200, 0.5); /* Custom focus outline for accessibility */
}

/* Secondary Button */
.btn-secondary {
    color: black ; /* Retain black text */
    border: none !important; /* Remove border */
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1) !important; /* Subtle shadow for depth */
    transition: transform 0.3s ease, box-shadow 0.3s ease, background 0.3s ease; /* Smooth transition for transform, shadow, and background */
    background: linear-gradient(135deg, #d4e8f9, #a8cde8); /* Keep the same gradient */
    padding: 12px 25px; /* Increased padding for better button size */
    border-radius: 8px; /* Rounded corners */
    cursor: pointer; /* Pointer cursor for interactivity */
    margin: 10px; /* Space between buttons */
}

.btn-secondary:hover {
    transform: translateY(-4px); /* Slight lift effect on hover */
    color: #cd2631 ;
    box-shadow: 0 6px 12px rgba(0, 0, 0, 0.15); /* Stronger shadow on hover */
    background: linear-gradient(135deg, #a8cde8, #d4e8f9); /* Slightly darker gradient on hover */
}

.btn-secondary:focus {
    outline: none; /* Remove default outline */
    box-shadow: 0 0 0 3px rgba(132, 185, 210, 0.5); /* Custom focus outline for accessibility */
}


        .btn:hover {
            opacity: 0.9;
        }
    </style>
</head>
<body>
    <!-- Bubble Animation -->
    <div class="bubbles">
        <div class="bubble"></div>
        <div class="bubble"></div>
        <div class="bubble"></div>
        <div class="bubble"></div>
        <div class="bubble"></div>
    </div>

    <!-- Include header -->
    <%- include('partials/index_header') %> <!-- Ensure this file path is correct and file exists -->

    <!-- Main Content Start -->
    <div class="container text-center py-5">
        <h1 class="wow fadeInDown">Welcome to Our Platform</h1>
        <p class="lead wow fadeInUp">Experience the best services with us.</p>
        <a href="/user/login" class="btn btn-primary btn-lg wow fadeInUp" data-wow-delay="0.3s">User Panel</a>
        <a href="/admin/login" class="btn btn-secondary btn-lg wow fadeInUp" data-wow-delay="0.6s">Admin Panel</a>
    </div>
    <!-- Main Content End -->

    <!-- Bootstrap JS, Popper.js, and jQuery -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <script src="../js/main.js"></script> <!-- Ensure this file exists -->
    <script src="https://cdn.jsdelivr.net/npm/wowjs@1.1.2/dist/wow.min.js"></script> <!-- WOW.js for animations -->
    <script>
        new WOW().init();
    </script>
</body>
</html>
