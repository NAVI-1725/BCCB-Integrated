<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Unauthorized Access</title>

  <!-- Tailwind CSS -->
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">

  <!-- Custom CSS -->
  <style>
    /* Background gradient */
    body {
      background: linear-gradient(135deg, #a8cde8, #a8e0e8); 
    }

    /* Transparent card with bubble animation */
    .card {
      max-width: 600px;
      margin: 0 auto;
      padding: 2rem;
      border-radius: 10px;
      background-color: rgba(255, 255, 255, 0.3); /* Transparent background */
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
      backdrop-filter: blur(10px); /* Glassmorphism effect */
      animation: floating 3s ease-in-out infinite;
      position: relative;
      overflow: hidden; /* Prevents bubbles from overflowing */
    }

    /* Floating effect for the card */
    @keyframes bubble-card {
      0%, 100% {
        transform: translateY(0);
      }
      50% {
        transform: translateY(-10px);
      }
    }

    .bubble {
            position: absolute;
            bottom: -270px;
            width: 40px;
            height: 40px;
            background: linear-gradient(135deg, #caefef, #c7e0ee);
            border-radius: 50%;
            animation: floatUp 10s infinite ease-in-out;
        }

        @keyframes floatUp {
            0% {
                transform: translateY(0) scale(1);
            }
            50% {
                transform: translateY(-600px) scale(1.2);
            }
            100% {
                transform: translateY(-1000px) scale(0.8);
            }
        }

        .bubble:nth-child(2) {
    width: 60px;
    height: 60px;
    animation-duration: 12s;
}

.bubble:nth-child(3) {
    left: 25%;
    width: 80px;
    height: 80px;
    animation-duration: 15s;
}

.bubble:nth-child(4) {
    left: 50%;
    width: 50px;
    height: 50px;
    animation-duration: 11s;
}

.bubble:nth-child(5) {
    left: 75%;
    width: 100px;
    height: 100px;
    animation-duration: 20s;
}


  </style>
</head>
<body class="text-gray-800 flex items-center justify-center min-h-screen">
  <div class="relative w-full">
    <!-- Floating Bubbles -->
    <div class="bubbles">
      <div class="bubble"></div>
      <div class="bubble"></div>
      <div class="bubble"></div>
      <div class="bubble"></div>
      <div class="bubble"></div>
  </div>

    <!-- Error Card -->
    <div class="card">
      <h1 class="text-3xl font-bold text-red-700 mb-4 text-center">Unauthorized Access</h1>
      <p class="text-gray-700 text-center">
        Oops! You are seeing this page because your session has expired or you are not logged in.
      </p>
      <ul class="list-disc list-inside mt-4 text-gray-700">
        <li>Your session might have timed out due to inactivity.</li>
        <li>You may have tried accessing a protected page without proper authentication.</li>
      </ul>
      <p class="text-gray-700 text-center mt-4">
        Please log in again to continue.
      </p>
      <div class="mt-6 flex justify-center">
        <a 
          href="/"
          class="px-6 py-2 rounded-lg bg-red-700 text-white font-semibold shadow hover:bg-black-500 transition-all">
          Go to Homepage
        </a>
      </div>
    </div>
  </div>
</body>
</html>
