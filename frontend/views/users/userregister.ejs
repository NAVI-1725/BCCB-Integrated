<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Register</title>
    <!-- Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        @keyframes move-background {
            from {
                background-position: 0 0;
            }
            to {
                background-position: 100% 100%;
            }
        }
        body::before {
            content: '';
            position: absolute;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, #d4f1f9, #a8e0e8); /* Gradient background */
            z-index: -1;
            animation: move-background 20s linear infinite;
        }
        .register-page {
            min-height: 85vh;
            display: flex;
            justify-content: center;
            align-items: center;
            background: linear-gradient(135deg, #d4f1f9, #a8e0e8);
            position: relative;
            overflow: hidden;
        }
        a {
            color: #0a879a;
            text-decoration: none;
            transition: color 0.3s ease;
        }
        a:hover {
            color: #6dc9e2; /* Slightly darker shade for hover effect */
            text-decoration: underline; /* Optional for hover styling */
        }
        @keyframes slide-in {
            from {
                transform: translateY(-50px);
                opacity: 0;
            }
            to {
                transform: translateY(0);
                opacity: 1;
            }
        }
        .register-box {
            width: 100%;
            max-width: 510px;
            background-color: rgba(255, 255, 255, 0.5);
            border-radius: 30px;
            padding: 35px;
            box-shadow: 0 20px 50px rgba(0, 0, 0, 0.3);
            backdrop-filter: blur(10px);
            animation: floating 3s ease-in-out infinite;
            position: relative;
            overflow: hidden;
        }
        .register-box:hover {
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.4);
        }
        @keyframes floating {
            0%, 100% {
                transform: translateY(0px);
            }
            50% {
                transform: translateY(-10px);
            }
        }
        .bubble {
            position: absolute;
            bottom: -20px;
            width: 30px;
            height: 30px;
            border-radius: 50%;
            background-color: rgba(255, 255, 255, 0.6);
            animation: float-bubbles 5s ease-in-out infinite;
        }
        @keyframes float-bubbles {
            0% {
                opacity: 0.7;
                transform: translateY(0) scale(1);
            }
            50% {
                opacity: 0.4;
                transform: translateY(-50px) scale(1.5);
            }
            100% {
                opacity: 0.7;
                transform: translateY(-100px) scale(1);
            }
        }
        .bubble:nth-child(1) { left: 10%; animation-duration: 6s; }
        .bubble:nth-child(2) { left: 20%; animation-duration: 7s; }
        .bubble:nth-child(3) { left: 30%; animation-duration: 5s; }
        .bubble:nth-child(4) { left: 40%; animation-duration: 8s; }
        .bubble:nth-child(5) { left: 50%; animation-duration: 6s; }
        .bubble:nth-child(6) { left: 60%; animation-duration: 7s; }
        .bubble:nth-child(7) { left: 70%; animation-duration: 6s; }
        .form-control {
            width: 80%;
            margin: 0 auto;
            transition: all 0.3s ease;
        }
        .form-control:focus {
            border-color: #007bff;
            box-shadow: 0 0 8px rgba(0, 123, 255, 0.5);
        }
        .btn-register {
            background-color: #5392a3;
            border-color: #0e6876;
            width: 200px; /* Specify the width */
            display: block; /* Ensures it behaves like a block element */
            margin: 0 auto; /* Centers the button */
            text-align: center; /* Ensures text is centered */
        }
        .btn-register:hover {
            transform: scale(1.05);
            color: #000;
            background: linear-gradient(135deg, #d4f1f9, #a8e0e8);
        }
    </style>
</head>
<body class="bg-gray-100">
    <header class="bg-blue-500 text-white p-4 text-center">
        <%- include('../partials/user_loginheader.ejs') %>
    </header>

    <div class="register-page">
        <div class="register-box">
            <h2 class="text-center text-2xl font-bold mb-4">User Registration</h2>

            <!-- Display error message, if any -->
            <% if (typeof error !== 'undefined' && error) { %>
                <div class="alert alert-danger" role="alert">
                    <%= error %>
                </div>
            <% } %>

            <form action="/user/register" method="post">
                <div class="form-group">
                    <label for="name" class="form-label">Full Name</label>
                    <input type="text" class="form-control mt-1" id="name" name="name" value="<%= typeof name !== 'undefined' ? name : '' %>" required>
                </div>
                <div class="form-group">
                    <label for="mobile" class="form-label">Mobile Number</label>
                    <input type="text" class="form-control mt-1" id="mobile" name="mobile" value="<%= typeof mobile !== 'undefined' ? mobile : '' %>" required>
                </div>
                <div class="form-group">
                    <label for="email" class="form-label">Email Address</label>
                    <input type="email" class="form-control mt-1" id="email" name="email" value="<%= typeof email !== 'undefined' ? email : '' %>" required>
                </div>
                <div class="form-group">
                    <label for="password" class="form-label">Password</label>
                    <input type="password" class="form-control mt-1" id="password" name="password" required>
                </div>
                <button type="submit" class="btn btn-primary btn-block mt-3 btn-register">Register</button>
                <p class="text-center mt-3">
                    <a href="/user/login" class="text-blue-500 hover:underline">Already have an account? Login here</a>
                </p>
            </form>                
        </div>
        <!-- Floating bubbles -->
        <div class="bubble"></div>
        <div class="bubble"></div>
        <div class="bubble"></div>
        <div class="bubble"></div>
        <div class="bubble"></div>
        <div class="bubble"></div>
        <div class="bubble"></div>
    </div>

    <footer class="bg-gray-800 text-white p-4 text-center">
        <%- include('../partials/footer.ejs') %>
    </footer>

    <!-- Bootstrap JS, Popper.js, and jQuery -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <script src="/js/main.js"></script>
</body>
</html>
